node_modules/
dist/
build/
.DS_Store
*.log
.env
.env.local

# vitest snapshot artifacts can stay tracked; coverage doesn't.
coverage/
.vitest-cache/

# pnpm
.pnpm-store/

# .harness/ — default policy is "private" (spec/format.md §7.1):
# everything is local-only; nothing is committed. When dogfooding switches
# to a "shared-snapshots" policy, narrow this to: .harness/lineage.sqlite,
# .harness/transcripts/, .harness/hooks/.
.harness/
