# `@harness/cli`

Scaffolded for prompt B2 — not implemented yet.

The `harness` command-line interface (`init`, `log`, `diff`, `tag`,
`branch`, `checkout`, `reindex`, `install-hook`) lives here once
[`@harness/core`](../core/) is solid and gated by the three test
gates documented in [B1's prompt](../../README.md#three-test-gates).
